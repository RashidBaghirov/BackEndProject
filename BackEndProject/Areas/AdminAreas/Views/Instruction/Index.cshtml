@model IEnumerable<BackEndProject.Entities.Instruction>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QualityDetails)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lining)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clean)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Only)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QualityDetails)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lining)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clean)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Only)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
